---
# defaults file for ansible-role-k3s

dashboardNodePort: 30443
helm_version: v2
node_label: unlabelled=true
nfs_exports:
    - path: "/root"
      export: "*(fsid=0,rw,async,no_root_squash,no_subtree_check,insecure)"